apiVersion: v1 
kind: Service
metadata:
  name: nginx-svc
  labels:
    env: dev
spec:
  selector:
    env: production
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30005