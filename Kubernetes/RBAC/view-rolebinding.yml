apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: pod-viewer-binding
  namespace: rbac-lab
subjects:
  - kind: ServiceAccount
    name: dev-user
    namespace: rbac-lab
roleRef:
  kind: Role 
  name: pod-viewer  # role name
  apiGroup: rbac.authorization.k8s.io
